# Python
.venv/
__pycache__/
*.pyc

# 构建产物
dist/
build/
*.spec.bak

# 运行时生成（索引可由 src.indexer 重建，历史为用户数据）
data/glyph_index.npz
data/history.jsonl
FontCop_data/

# 字体文件（体积大且含授权差异，用 tools/fetch_fonts.py 重建）
fonts/files/
fonts/system_fonts.json
fonts/symbols.json
fonts/coverage.json

# 临时/日志/调试残留（误重定向、探针、自检截图）
*.log
nul
_tmp_*
_probe*
*.bak
*.bak3

# 系统
.DS_Store

# AtomCode local-scope configs (machine-specific)
.atomcode/
.anchors/
